"""Prompt 模板：测试用例生成

基于需求分析结果，生成结构化测试用例 JSON。
输出格式严格匹配 Xmind 导入所需的 title+children 嵌套结构。
"""

# 按单个模块生成测试用例（用于分批调用，避免超出模型输出限制）
# 用 %s 占位
MODULE_PROMPT = """请根据以下需求分析信息，为指定功能模块设计测试用例。

## 完整需求分析
%s

## 当前要生成用例的模块
模块名称：%s
模块信息：%s

## 要求

1. 只为当前指定的模块生成用例，不要生成其他模块的用例
2. 每个用例包含：优先级(P0-P3)、前置条件、操作步骤、预期结果
3. 覆盖正常流程、边界值、异常场景
4. 用例标题精简，不超过25字
5. 生成 3-8 条用例

## 输出格式

严格输出以下 JSON 结构（只输出 topics 数组的内容，即一个模块对象）：

{
  "title": "模块名称",
  "children": [
    {
      "title": "TC001-用例标题",
      "children": [
        {
          "title": "前置：xxx",
          "children": [
            {
              "title": "步骤：1.xxx 2.xxx",
              "children": [
                { "title": "预期：xxx" }
              ]
            }
          ]
        }
      ]
    }
  ]
}

只输出 JSON，不要输出其他内容。确保 JSON 完整闭合。"""

# 完整生成 prompt（仅作为 fallback，当分析结果无法拆分模块时使用）
# 用 %s 占位
FULL_PROMPT = """请根据以下需求分析结果，设计功能测试用例。

## 需求分析结果
%s

## 要求

1. 按功能模块划分用例
2. 每个用例包含：优先级(P0-P3)、前置条件、操作步骤、预期结果
3. 覆盖正常流程、边界值、异常场景
4. 用例标题精简，不超过25字
5. 每个模块控制在 3-5 条用例
6. 步骤和预期结果必须简短，每个不超过 20 字

## 输出格式

严格输出以下 JSON 结构（每个节点只有 title 和 children 两个字段）：

{
  "title": "测试用例",
  "filename": "测试用例",
  "topics": [
    {
      "title": "模块名称",
      "children": [
        {
          "title": "TC001-用例标题",
          "children": [
            {
              "title": "前置：xxx",
              "children": [
                {
                  "title": "步骤：1.xxx 2.xxx",
                  "children": [
                    { "title": "预期：xxx" }
                  ]
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}

只输出 JSON，不要输出其他内容。确保 JSON 完整闭合。"""
